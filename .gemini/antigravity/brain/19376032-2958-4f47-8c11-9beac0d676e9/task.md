# Animated Grid Blueprint Background Tasks

- [x] Configure slow flowing blueprint grid animations (`globals.css`)
- [x] Hide the desktop consultation button inside the mobile header using `:global(.desktop-cta)` selector (`Header.js`)
- [x] Remove solid background color overrides from all page wrappers so the animated grid displays on all pages (`home-client.js`, `about-client.js`, `team-client.js`, `services-client.js`, `contact-client.js`)
- [x] Run dev server and confirm hot-reloaded outputs (`npm run dev`)
